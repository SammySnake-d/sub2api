# import-count-ledger

状态: **未执行**

这是一份人判记录位。机器只能核到本文件存在且非空,核不了结论本身。
判定人在此处填写: 日期 / 执行了什么 / 观察到什么 / 结论(通过或不通过) / 签名。

核对内容: 记下来源总数、导入数、有意保留数, 确认 导入数 + 保留数 == 来源总数。
差额本身就是证据: 不等于, 说明有账号既没被导入也没被有意保留 —— 它在中间蒸发了。
三个数字都要写实际值, 不写「一致」。

未填写前,本条验收标准视为未通过。
