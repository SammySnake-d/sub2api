# secrets-hygiene

状态: **未执行**

这是一份人判记录位。机器只能核到本文件存在且非空,核不了结论本身。
判定人在此处填写: 日期 / 执行了什么 / 观察到什么 / 结论(通过或不通过) / 签名。

核对内容: 逐项确认 JWT secret、管理员口令、数据库口令、resin token 四个值
都是部署时随机生成的, 没有一个等于仓库默认值; 再确认它们不出现在仓库、
日志和测试输出里。
**本文件禁止写入任何 secret 明文**。只写: 核对了哪四项 / 用什么方法确认
(例如比对哈希、比对长度与来源) / 结论。写进来的明文本身就会让这条判据失效。

未填写前,本条验收标准视为未通过。
